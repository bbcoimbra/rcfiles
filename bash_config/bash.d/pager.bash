### less options
# -i case-insensitive search
# -F quit if one screen
# -X don't initialize termcap (like clean screen)
# -M long prompt (verbose)
# -S don't wrap lines
# -R raw mode
# -x set tabstop positions
export PAGER=less
export LESS="-iFXMSRx4"
export LESSCHARSET="UTF-8"
